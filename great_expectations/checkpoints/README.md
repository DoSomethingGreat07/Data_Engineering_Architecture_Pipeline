# Great Expectations Checkpoints

This project uses Python-driven Validation Definitions for local and Databricks execution.

The `checkpoints/` directory is kept for production checkpoint materialization in later orchestration phases.
For Phase 5, validation outputs are written to:

- `great_expectations/results/`
- `great_expectations/uncommitted/data_docs/`

